# Copy this file to .env and fill in your API keys
# cp .env.example .env

# Required: Used for embeddings (text-embedding-3-small) and LLM calls (gpt-5-mini)
OPENAI_API_KEY=sk-...

# Optional: Only needed if you want to use Claude models
# ANTHROPIC_API_KEY=sk-ant-...

# Optional: Only needed for smolagents with HuggingFace Inference API
# HF_TOKEN=hf_...
